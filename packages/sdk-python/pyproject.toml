[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[project]
name = "tradeos-public-intel"
version = "0.1.0"
description = "Python SDK for TradeOS public intelligence."
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
authors = [{ name = "TradeOS contributors" }]
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]

