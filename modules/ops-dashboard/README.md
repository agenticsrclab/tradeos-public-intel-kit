# TradeOS Local Ops Dashboard Module

Reusable snapshot contract for local recommendations, approvals, adapter
status, audit events, feedback queues, and kill switch state.

This module intentionally exposes a data contract rather than a heavy UI. The
symbol cockpit web app consumes the same shape and future apps can embed it.

